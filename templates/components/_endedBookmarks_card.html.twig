<div class="card" style="width: 18rem;">
  <div class="card-body">
    <h5 class="card-title">{{ bookmark.name }}</h5>
    <h6 class="card-subtitle mb-2 text-muted">{{ bookmark.type }}</h6>
    <p class="card-text">{{ bookmark.comment }}</p>
    <p class="card-text">{{ bookmark.bookmark }}</p>
    <p class="card-text">Started at : {{ bookmark.startedAt }}</p>
    <p class="card-text">Ended at : {{ bookmark.endedAt }}</p>
  </div>
</div>